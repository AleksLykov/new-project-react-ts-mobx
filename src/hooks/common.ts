export const value = 123;

// export const useSnackBar = (): [(item: SnackBarItem) => void, Item[]] => {
//   const { items, addItem } = useContext(SnackBarContext);
//   return [addItem, items];
// };
